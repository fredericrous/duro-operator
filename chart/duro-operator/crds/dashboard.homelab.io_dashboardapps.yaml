---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dashboardapps.dashboard.homelab.io
spec:
  group: dashboard.homelab.io
  names:
    kind: DashboardApp
    listKind: DashboardAppList
    plural: dashboardapps
    shortNames:
    - dapp
    singular: dashboardapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.category
      name: Category
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardApp is the Schema for the dashboardapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardAppSpec defines the desired state of DashboardApp
            properties:
              category:
                description: Category groups the app in the dashboard
                enum:
                - media
                - ai
                - productivity
                - development
                - admin
                type: string
              groups:
                description: Groups defines which LDAP/OIDC groups can see this app
                  (OR logic)
                items:
                  type: string
                minItems: 1
                type: array
              icon:
                description: Icon is the raw SVG string for the app icon
                type: string
              name:
                description: Name is the display name of the application
                type: string
              priority:
                default: 100
                description: Priority controls sort order within a category (lower
                  = first)
                type: integer
              url:
                description: URL is the application URL
                type: string
            required:
            - category
            - groups
            - icon
            - name
            - url
            type: object
          status:
            description: DashboardAppStatus defines the observed state of DashboardApp
            properties:
              conditions:
                description: Conditions represent the current state of the DashboardApp
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncedAt:
                description: LastSyncedAt is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              ready:
                description: Ready indicates if the app has been synced to the ConfigMap
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
